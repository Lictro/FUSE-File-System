#include "fuse.h"
#include "bits.h"
#include <stdlib.h>

int main(int argc, char *argv[]){
    system ("CLS");
    CLI();

    /*int x = 3;
    for(int i=0; i < 32; i++){
        printf("bit %d: %d\n",i,BIT_GET(x,i));
    }
    printf("Valor: %d\n",x);
    BIT_CLEAR(x,1);
    printf("Nuevo Valor: %d\n",x);
    BIT_SET(x,2);
    printf("Nuevo Valor: %d\n",x);*/
    return 0;
}